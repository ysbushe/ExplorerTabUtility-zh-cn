<Window x:Class="ExplorerTabUtility.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ExplorerTabUtility.UI.Views"
        xmlns:managers="clr-namespace:ExplorerTabUtility.Managers"
        xmlns:strings="clr-namespace:ExplorerTabUtility.Strings"
        Title="{x:Static strings:Res.AppTitle}"
        Width="850" Height="600"
        MinWidth="700" MinHeight="175"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        TextBlock.Foreground="{StaticResource TextPrimaryBrush}"
        TextElement.Foreground="{StaticResource TextPrimaryBrush}">

    <Border Background="{StaticResource BackgroundGradientBrush}"
            BorderBrush="{StaticResource BorderGradientBrush}"
            CornerRadius="5"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border x:Name="TitleBar" Grid.Row="0" Background="{StaticResource TitleBarBrush}" CornerRadius="4, 4, 0, 0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Menu Button -->
                    <ToggleButton x:Name="DrawerButton" Grid.Column="0" Style="{StaticResource ToggleButtonIconStyle}" Margin="6,0,0,0"
                                  Content="&#x2630;" Focusable="False" ToolTip="{x:Static strings:Res.TipOpenAppMenu}" />

                    <!-- Window Title -->
                    <TextBlock Grid.Column="1" Text="{x:Static strings:Res.AppTitle}" VerticalAlignment="Center" Margin="8,0"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontWeight="SemiBold"
                               FontSize="14" />

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="MinimizeButton" Style="{StaticResource IconButtonStyle}" Content="&#x2212;" Focusable="False" />
                        <Button x:Name="MaximizeButton" Focusable="False">
                            <Button.Style>
                                <Style BasedOn="{StaticResource IconButtonStyle}" TargetType="Button">
                                    <Setter Property="Content" Value="&#x2610;" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding WindowState, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                            Value="Maximized">
                                            <Setter Property="Content" Value="&#x2750;" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button x:Name="CloseButton" Style="{StaticResource IconButtonStyle}" Content="&#x2715;" Focusable="False"
                                Margin="0,0,8,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <!-- Custom Drawer Implementation -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Menu Drawer -->
                    <Border Grid.Column="0"
                            Background="{StaticResource CardGradientBrush}"
                            BorderBrush="{StaticResource BorderGradientBrush}"
                            BorderThickness="0,0,1,0"
                            CornerRadius="0,0,0,4"
                            Width="0">
                        <DockPanel>
                            <ListBox x:Name="NavigationList" DockPanel.Dock="Top" SelectedIndex="0"
                                     Style="{StaticResource NavigationListBoxStyle}">
                                <ListBox.Resources>
                                    <Storyboard x:Key="CloseDrawerStoryboard">
                                        <BooleanAnimationUsingKeyFrames Storyboard.TargetName="DrawerButton"
                                                                        Storyboard.TargetProperty="IsChecked">
                                            <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="False" />
                                        </BooleanAnimationUsingKeyFrames>
                                    </Storyboard>
                                </ListBox.Resources>
                                <ListBox.Triggers>
                                    <EventTrigger RoutedEvent="ListBox.MouseLeftButtonDown">
                                        <BeginStoryboard Storyboard="{StaticResource CloseDrawerStoryboard}"/>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="ListBox.PreviewMouseDoubleClick">
                                        <BeginStoryboard Storyboard="{StaticResource CloseDrawerStoryboard}"/>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="ListBox.KeyDown">
                                        <BeginStoryboard Storyboard="{StaticResource CloseDrawerStoryboard}"/>
                                    </EventTrigger>
                                </ListBox.Triggers>
                                <ListBoxItem ToolTip="{x:Static strings:Res.TipConfigureShortcuts}">
                                    <TextBlock Text="{x:Static strings:Res.NavShortcuts}" VerticalAlignment="Center" />
                                </ListBoxItem>
                                <ListBoxItem ToolTip="{x:Static strings:Res.TipConfigureSettings}">
                                    <TextBlock Text="{x:Static strings:Res.NavPreferences}" VerticalAlignment="Center" />
                                </ListBoxItem>
                                <ListBoxItem ToolTip="{x:Static strings:Res.TipAboutApp}">
                                    <TextBlock Text="{x:Static strings:Res.NavAbout}" VerticalAlignment="Center" />
                                </ListBoxItem>
                            </ListBox>
                        </DockPanel>

                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.3" Color="{StaticResource ShadowColor}" />
                        </Border.Effect>

                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=DrawerButton}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"
                                                                     BeginTime="0:0:0.1" />
                                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="165" Duration="0:0:0.3">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseOut" />
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2" />
                                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="0" Duration="0:0:0.3">
                                                        <DoubleAnimation.EasingFunction>
                                                            <CubicEase EasingMode="EaseIn" />
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                    <!-- Main Content -->
                    <Grid Grid.Column="1">
                        <!-- Tab Control -->
                        <TabControl Style="{StaticResource CustomTabControlStyle}"
                                    SelectedIndex="{Binding SelectedIndex, ElementName=NavigationList}">
                            <TabControl.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.2"
                                                  Color="{StaticResource ShadowColor}" />
                            </TabControl.Effect>
                            <!-- Shortcuts Tab -->
                            <TabItem Header="{x:Static strings:Res.NavShortcuts}" Visibility="Collapsed">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <!-- Toolbar -->
                                    <DockPanel Grid.Row="0" Margin="16,8" LastChildFill="False">
                                        <!-- Action Buttons -->
                                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                                            <Button x:Name="BtnNewProfile" Content="{x:Static strings:Res.BtnNew}" Style="{StaticResource FlatButtonStyle}"
                                                    ToolTip="{x:Static strings:Res.TipCreateProfile}" />
                                            <Button x:Name="BtnImport" Content="{x:Static strings:Res.BtnImport}" Style="{StaticResource FlatButtonStyle}"
                                                    ToolTip="{x:Static strings:Res.TipImportProfiles}" />
                                            <Button x:Name="BtnExport" Content="{x:Static strings:Res.BtnExport}" Style="{StaticResource FlatButtonStyle}"
                                                    ToolTip="{x:Static strings:Res.TipExportProfiles}" />
                                            <Button x:Name="BtnSave" Content="{x:Static strings:Res.BtnSave}" Style="{StaticResource FlatButtonStyle}"
                                                    ToolTip="{x:Static strings:Res.TipSaveProfiles}" />
                                        </StackPanel>

                                        <!-- Save on Exit Toggle -->
                                        <CheckBox DockPanel.Dock="Right" x:Name="CbAutoSaveProfiles"
                                                  Content="{x:Static strings:Res.CbAutoSave}" Style="{StaticResource CustomCheckBoxStyle}"
                                                  ToolTip="{x:Static strings:Res.TipAutoSave}" />
                                    </DockPanel>

                                    <!-- Profiles Panel -->
                                    <ScrollViewer Grid.Row="1" Margin="16,8,4,4"
                                                  VerticalScrollBarVisibility="Visible"
                                                  Style="{StaticResource CustomScrollViewerStyle}">
                                        <StackPanel x:Name="ProfilesPanel" />
                                    </ScrollViewer>
                                </Grid>
                            </TabItem>

                            <!-- Preferences Tab -->
                            <TabItem Header="{x:Static strings:Res.NavPreferences}" Visibility="Collapsed">
                                <StackPanel Margin="16, 8">
                                    <GroupBox Header="{x:Static strings:Res.GrpAppSettings}"
                                              Style="{StaticResource CustomGroupBoxStyle}">
                                        <StackPanel Margin="8">
                                            <CheckBox x:Name="CbAutoUpdate" Content="{x:Static strings:Res.CbAutoUpdate}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipAutoUpdate}" />
                                            <CheckBox x:Name="CbThemeIssue" Content="{x:Static strings:Res.CbThemeIssue}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipThemeIssue}" />
                                            <CheckBox x:Name="CbSaveClosedHistory" Content="{x:Static strings:Res.CbSaveClosedHistory}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipSaveClosedHistory}" />
                                            <CheckBox x:Name="CbRestorePreviousWindows" Content="{x:Static strings:Res.CbRestorePrevious}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipRestorePrevious}" />
                                            <CheckBox x:Name="CbHideTrayIcon" Content="{x:Static strings:Res.CbHideTrayIcon}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipHideTrayIcon}" />
                                        </StackPanel>
                                    </GroupBox>

                                    <GroupBox Header="{x:Static strings:Res.GrpLanguage}"
                                              Style="{StaticResource CustomGroupBoxStyle}"
                                              Margin="0,8,0,0">
                                        <StackPanel Margin="8">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="{x:Static strings:Res.LblLanguage}"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0"
                                                           MinWidth="100" />
                                                <ComboBox x:Name="CbLanguage"
                                                          Style="{StaticResource CustomComboBoxStyle}"
                                                          Width="150"
                                                          ToolTip="{x:Static strings:Res.TipLanguage}">
                                                    <ComboBoxItem Content="Auto" Tag="Auto" />
                                                    <ComboBoxItem Content="English" Tag="en-US" />
                                                    <ComboBoxItem Content="简体中文" Tag="zh-CN" />
                                                </ComboBox>
                                            </StackPanel>
                                            <TextBlock Text="{x:Static strings:Res.TipLanguageRestart}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="11"
                                                       Margin="0,4,0,0" />
                                        </StackPanel>
                                    </GroupBox>

                                    <GroupBox Header="{x:Static strings:Res.GrpMouseFeatures}"
                                              Style="{StaticResource CustomGroupBoxStyle}"
                                              Margin="0,8,0,0">
                                        <StackPanel Margin="8">
                                            <CheckBox x:Name="CbDoubleClickEmptySpace"
                                                      Content="{x:Static strings:Res.CbDoubleClickEmptySpace}"
                                                      Style="{StaticResource CustomCheckBoxStyle}"
                                                      ToolTip="{x:Static strings:Res.TipDoubleClickEmptySpace}" />
                                        </StackPanel>
                                    </GroupBox>
                                </StackPanel>
                            </TabItem>

                            <!-- About Tab -->
                            <TabItem Header="{x:Static strings:Res.NavAbout}" Visibility="Collapsed">
                                <local:AboutView />
                            </TabItem>
                        </TabControl>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="{StaticResource ControlBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0" CornerRadius="0,0,4,4">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"  Margin="8,4">
                        <!-- Window Hook Status -->
                        <StackPanel Orientation="Horizontal" Margin="8,0">
                            <Ellipse Width="12" Height="12" Margin="0,0,6,0" StrokeThickness="1">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource StatusInactiveBrush}" />
                                        <Setter Property="Stroke" Value="{StaticResource BorderBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=(managers:SettingsManager.IsWindowHookActive)}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource StatusActiveBrush}" />
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8"
                                                                          Color="{StaticResource StatusActiveColor}" Opacity="0.7" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{x:Static strings:Res.StatusWindowHook}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>

                        <!-- Reuse Tabs Status -->
                        <StackPanel Orientation="Horizontal" Margin="8,0">
                            <Ellipse Width="12" Height="12" Margin="0,0,6,0" StrokeThickness="1">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource StatusInactiveBrush}" />
                                        <Setter Property="Stroke" Value="{StaticResource BorderBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=(managers:SettingsManager.ReuseTabs)}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource StatusActiveBrush}" />
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8"
                                                                          Color="{StaticResource StatusActiveColor}" Opacity="0.7" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{x:Static strings:Res.StatusReuseTabs}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>

                        <!-- Keyboard Hook Status -->
                        <StackPanel Orientation="Horizontal" Margin="8,0">
                            <Ellipse Width="12" Height="12" Margin="0,0,6,0" StrokeThickness="1">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource StatusInactiveBrush}" />
                                        <Setter Property="Stroke" Value="{StaticResource BorderBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=(managers:SettingsManager.IsKeyboardHookActive)}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource StatusActiveBrush}" />
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8"
                                                                          Color="{StaticResource StatusActiveColor}" Opacity="0.7" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{x:Static strings:Res.StatusKeyboardHook}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>

                        <!-- Mouse Hook Status -->
                        <StackPanel Orientation="Horizontal" Margin="8,0">
                            <Ellipse Width="12" Height="12" Margin="0,0,6,0" StrokeThickness="1">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource StatusInactiveBrush}" />
                                        <Setter Property="Stroke" Value="{StaticResource BorderBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=(managers:SettingsManager.IsMouseHookActive)}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource StatusActiveBrush}" />
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect ShadowDepth="0" BlurRadius="8"
                                                                          Color="{StaticResource StatusActiveColor}" Opacity="0.7" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{x:Static strings:Res.StatusMouseHook}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
